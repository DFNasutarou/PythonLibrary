{
  "mo": {
    "prefix": "mo",
    "body": [
      "class Mo:",
      "  def __init__(self, N, Query):",
      "    self.N = N",
      "    self.Query = Query",
      "    self.Q = len(Query)",
      "    n = int(N ** (1 / 2))",
      "    B = [[] for _ in range(-(-N // n))]",
      "    i = 0",
      "    for que in Query:",
      "      t = que[0]",
      "      p = (t - 1) // n",
      "      B[p].append(que + [i])",
      "      i += 1",
      "    for i in range(len(B)):",
      "      B[i].sort(key = lambda x:x[1])",
      "      if i % 2:",
      "        B[i].reverse()",
      "    self.ini()",
      "    for b in B:",
      "      for x, y, i in b:",
      "        if self.l > x:",
      "          for _ in range(self.l - x):",
      "            self.l_minus()",
      "        if self.r > y:",
      "          for _ in range(self.r - y):",
      "            self.r_minus()",
      "        if self.r < y:",
      "          for _ in range(y - self.r):",
      "            self.r_plus()",
      "        if self.l < x:",
      "          for _ in range(x - self.l):",
      "            self.l_plus()",
      "        self.anser(i)",
      "  def getAns(self):",
      "    return self.Ans",
      "  def anser(self, i):",
      "    self.Ans[i] = self.ans",
      "  def ini(self):",
      "    self.l, self.r = 1, 1",
      "    self.Ans = [0] * self.Q",
      "    self.ans = 0",
      "  def l_plus(self):",
      "    self.l += 1",
      "  def l_minus(self):",
      "    self.l -= 1",
      "  def r_plus(self):",
      "    self.r += 1",
      "  def r_minus(self):",
      "    self.r -= 1",
      "",
      "# usage: m = Mo(N, Query); m.run(add, sub, ans_callback)"
    ],
    "description": "Mo's algorithm (区間クエリのオフライン処理、√N ブロック並べ替え)",
    "scope": "python"
  },
  "rangeaffine": {
    "scope": "python",
    "prefix": "rangeaffine",
    "description": "区間関数アフィン RangeAffine (range_min / max / sum のヘルパ集)",
    "body": [
      "class RangeAffine:",
      "  @ staticmethod",
      "  def range_min_sum_list(L):",
      "    \"\"\"",
      "    最小和",
      "    N+1個の点が一列に並んでいて、点間の重みのリストL(要素数N)が与えられたとき、",
      "    各点iについて、以下の答えを求めて、答えのリストを返す。",
      "     各点j(i!=j)について、i-j_path上の重みの最小値の和",
      "     O(N)",
      "    \"\"\"",
      "    X= RangeAffine._range_min_sum_list(L)",
      "    Y = RangeAffine._range_min_sum_list(L[::-1])",
      "    for i in range(len(L)): X[i] += Y[-1-i]",
      "    return X",
      "  ",
      "  @ staticmethod",
      "  def range_max_sum_list(L):",
      "    \"\"\"",
      "    最大和",
      "    N+1個の点が一列に並んでいて、点間の重みのリストL(要素数N)が与えられたとき、",
      "    各点iについて、以下の答えを求めて、答えのリストを返す。",
      "     各点j(i!=j)について、i-j_path上の重みの最大値の和",
      "     O(N)",
      "    \"\"\"",
      "    X= RangeAffine._range_max_sum_list(L)",
      "    Y = RangeAffine._range_max_sum_list(L[::-1])",
      "    for i in range(len(L)): X[i] += Y[-1-i]",
      "    return X",
      "",
      "  @ staticmethod",
      "  def _range_min_sum_list(L):",
      "    \"\"\"",
      "    各iについて、0 <= j < i のrange_min(j, i)の合計を求める",
      "    \"\"\"",
      "    N = len(L)",
      "    D = []",
      "    ans = [0] * (N + 1)",
      "    sm = 0",
      "    for i in range(N):",
      "      n = 1",
      "      while D:",
      "        if D[-1][0] >= L[i]:",
      "          sm -= D[-1][0] * D[-1][1]",
      "          n += D[-1][1]",
      "          D.pop()",
      "        else: break",
      "      sm += L[i] * n",
      "      D.append([L[i], n])",
      "      ans[i + 1] = sm",
      "    return ans",
      "  ",
      "  @ staticmethod",
      "  def _range_max_sum_list(L):",
      "    \"\"\"",
      "    各iについて、0 <= j < i のrange_max(j, i)の合計を求める",
      "    \"\"\"",
      "    N = len(L)",
      "    D = []",
      "    ans = [0] * (N + 1)",
      "    sm = 0",
      "    for i in range(N):",
      "      n = 1",
      "      while D:",
      "        if D[-1][0] <= L[i]:",
      "          sm -= D[-1][0] * D[-1][1]",
      "          n += D[-1][1]",
      "          D.pop()",
      "        else: break",
      "      sm += L[i] * n",
      "      D.append([L[i], n])",
      "      ans[i + 1] = sm",
      "    return ans",
      "",
      "# usage: m = RangeAffine.range_min_sum_list(L)"
    ]
  },
  "imos2d": {
    "scope": "python",
    "prefix": "imos2d",
    "description": "2D いもす法 (矩形加算 + 累積和、計算 O(HW))",
    "body": [
      "class Imos2D:",
      "  def __init__(self, H, W) -> None:",
      "    self.H = H",
      "    self.W = W",
      "    self.Data = [[0] * (W + 1) for _ in range(H + 1)]",
      "  def add(self, h1, w1, h2, w2, value) -> None:",
      "    # 指定範囲に加算",
      "    h1 = max(h1, 0)",
      "    w1 = max(w1, 0)",
      "    h2 = min(h2, self.H)",
      "    w2 = min(w2, self.W)",
      "    self.Data[h1][w1] += value",
      "    self.Data[h1][w2] -= value",
      "    self.Data[h2][w1] -= value",
      "    self.Data[h2][w2] += value",
      "  def get_data(self):",
      "    # 現在の結果を取得",
      "    ret = [d[:self.W] for d in self.Data[:self.H]]",
      "    for i in range(self.H):",
      "      for j in range(1, self.W):",
      "        ret[i][j] += ret[i][j - 1]",
      "    for j in range(self.W):",
      "      for i in range(1, self.H):",
      "        ret[i][j] += ret[i - 1][j]",
      "    return ret",
      "",
      "# usage: im = Imos2D(H, W); im.add(r1, c1, r2, c2, v); G = im.calc()"
    ]
  },
  "squaresplit": {
    "scope": "python",
    "prefix": "squaresplit",
    "description": "平方分割 SquareSpilit (区間更新 + 区間取得、gf と組み合わせ前提)",
    "body": [
      "class SquareSpilit:",
      "  class Query_solve:",
      "    def __init__(self, N):",
      "      self.data = [1] * N # 現在の値",
      "      self.time = [-1] * N # 現在の値が更新された時間",
      "      self.effort = [1] * N # 自分が影響を与える値",
      "      self.effort_time = [-1] * N #自分が影響を与える値が更新された時間",
      "      self.count = 0",
      "    def effort_other(self, a, b, value):",
      "      \"\"\"",
      "      aが更新されたときのbへの影響を記述する",
      "      \"\"\"",
      "      self.data[b] = self.effort[a]",
      "      self.time[b] = self.effort_time[a]",
      "    def solve_myself(self, a, value):",
      "      \"\"\"",
      "      aが更新されたときのa自身への影響を記述する",
      "      \"\"\"",
      "      ans = self.data[a]",
      "      self.data[a] = value",
      "      self.time[a] = self.count",
      "      self.effort[a] = value",
      "      self.effort_time[a] = self.count",
      "      self.count += 1",
      "      return ans",
      "    def solve_other(self, a, b, value):",
      "      \"\"\"",
      "      aが更新されるとき、bの更新の影響を取りに行く",
      "      \"\"\"",
      "      if self.time[a] < self.effort_time[b]:",
      "        self.time[a] = self.effort_time[b]",
      "        self.data[a] = self.effort[b]",
      "    def get_index(self, q):",
      "      \"\"\"",
      "      クエリから変更されたノードのインデックスを取得",
      "      \"\"\"",
      "      return q[0]",
      "    def get_value(self, q):",
      "      \"\"\"",
      "      クエリから変更値を取得",
      "      複数ある場合はq[1:]に変える",
      "      \"\"\"",
      "      return q[1]",
      "    def debug(self):",
      "      print(\"---debug---\")",
      "      print(self.data)",
      "      print(self.time)",
      "      print(self.effort)",
      "      print(self.effort_time)",
      "  def __init__(self, gf: GraphData, qs: Query_solve):",
      "    self.qs = qs",
      "    self.effort_list = [[] for _ in range(gf.N)]",
      "    self.solve_list = [[] for _ in range(gf.N)]",
      "    m = (gf.N ** (1 / 2)) // 1",
      "    for i in range(gf.N):",
      "      if len(gf.adj_list[i]) < m:",
      "        self.effort_list[i] = gf.adj_list[i]",
      "      else:",
      "        for ind in gf.adj_list[i]:",
      "          self.solve_list[ind].append(i)",
      "  def query(self, q):",
      "    ind = self.qs.get_index(q)",
      "    value = self.qs.get_value(q)",
      "    for b in self.solve_list[ind]: # ソートが必要なケースがあるかも？",
      "      self.qs.solve_other(ind, b, value)",
      "    ans = self.qs.solve_myself(ind, value)",
      "    for b in self.effort_list[ind]:",
      "      self.qs.effort_other(ind, b, value)",
      "    return ans",
      "",
      "# usage: sq = SquareSpilit(N); sq.update(l, r, v); a = sq.get(l, r)"
    ]
  }
}
